import React from "react"

export default function Saved() {
    return (
        <div>
           <p>This is the page where the user will be able to view their saved apartments.</p> 
        </div>
    )
}