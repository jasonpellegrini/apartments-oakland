import React from "react"
import './hero.css'

export default function Hero() {
    return (
        <div id="hero">
            <main role="main">
                <h1 id="heroText">Find out the facts for your next South Oakland apartment.</h1>
                <button id="heroButton">GET STARTED</button>
            </main>
        </div>
    )
}